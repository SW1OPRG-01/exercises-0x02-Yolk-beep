/*Reconstruct exercise 1 where the sum now is calculated by a function implemented in an
h- and a cpp-file*/

#include <iostream>
#include "sum.h"

using namespace std;

int main() {
    int x;
    cin>>x;
    int sum=summies(x);
    return 0;
}